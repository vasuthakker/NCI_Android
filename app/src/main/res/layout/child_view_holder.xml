<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_marginRight="15dp"
    android:layout_marginLeft="15dp"
    android:padding="20dp"
    android:background="@drawable/bg_faq_rectangle">

    <TextView
        android:id="@+id/question_number"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:textSize="17dp"
        android:text="I have downloaded the app but when I am trying to register,it simply stops me there.What should I do?"
        android:textStyle="bold"
        android:textColor="@android:color/black"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:layout_marginLeft="10dp"/>
    <TextView
        android:id="@+id/answer_number"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textSize="17dp"
        android:text="Check if you are eligible for app.You first need to register at nci center for registring for the app."
        android:layout_below="@+id/question_number"
        android:layout_marginLeft="-38dp"
        app:layout_constraintLeft_toLeftOf="@+id/question_number"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@+id/question_number"
        app:layout_constraintRight_toRightOf="@+id/question_number"
        />
</android.support.constraint.ConstraintLayout>